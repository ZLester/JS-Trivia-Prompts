function impossible () {
    var a;

    if (a == 1 && a == 2 && a == 3) {
        console.log('foo');
    } else {
        console.log('bar')
    }
}

impossible();

// Without modifying the if statement, 
// is it possible to make the above log 'foo'?

// If so, how?
